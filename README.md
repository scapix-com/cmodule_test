Example project for [cmodule](https://github.com/scapix-com/cmodule) package manager:

```bash
git clone https://github.com/scapix-com/cmodule_test
cd cmodule_test
cmake -B build
cmake --build build
```
